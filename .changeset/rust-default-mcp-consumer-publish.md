---
"@sylphx/coderag-mcp": minor
"@sylphx/coderag": patch
---

Ship Rust-default MCP consumer packaging: fail-closed `coderag-mcp` launcher, staged `bin/native` rmcp server, and restore the Release workflow so npm can publish a SHA-bound artifact from main.
