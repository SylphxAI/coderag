# Locus — local code search

```bash
npx @sylphx/locus --root=/absolute/path/to/project
```

Primary: `codebase_search` (and related search ops owned by this package).
Not architecture path/impact (use Spine). Not filesystem writes.
